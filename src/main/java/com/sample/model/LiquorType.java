package com.sample.model;

/**
 * 3.2. Creating the LiquorType enum class
 * We’ll use an enum class to hold the liquor types of our app.
 * Create a LiquorType.java class inside the src/main/java/ with a package name com.sample.model.
 */
public enum LiquorType {
    WINE,BEER,WHISKY
}
